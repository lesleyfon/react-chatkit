import React from 'react'
import ScrollToBottom from 'react-scroll-to-bottom';



function MessageList() {
    const defaultStyles = {
        height: '600px',
        width: '400px'
    }
    return (
        <ScrollToBottom styles={defaultStyles}>
            
        </ScrollToBottom>
    )
}

export default MessageList
